import { ScannerContent } from "@/components/scanner/ScannerContent";

export const metadata = {
  title: "Scan QR - Presensi Siswa Sekolah Dasar",
};

export default function ScanPage() {
  return <ScannerContent />;
}
